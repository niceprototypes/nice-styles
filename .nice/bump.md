[2026-06-03 20:48] patch: Split module.json token source into per-group files under tokens/modules/
[2026-06-05 13:44] minor: Add breakpoint axis to component tokens — components/*.json now accept $breakpoints, parallel to $themes
[2026-06-05 14:44] minor: Support alt-theme names beyond night — any $themes key emits a [data-theme] pin (modules and components)
[2026-06-08 23:25] minor: Add base-less token shorthands — bare (--np--color, --np--gap) and dimensional (--np--color--night, --np--font-size--desktop) aliases for module + component tokens. Shipped in tokens.css (auto) and standalone "nice-styles/shorthand.css"
[2026-06-09 02:35] major: Reorder button/input status tokens to property-then-state so `base` is terminal, matching the module shape. Renames emitted vars: --np--button--status--primary--base--background-color → --np--button--status--primary--background-color--base (same for input). Path lookups must use [status, type, property, state]
[2026-06-09 19:09] minor: Add JS-only font loading — setFonts({ googleFonts, adobeFonts }) injects the same <head> preconnect + stylesheet links as StylesProvider. Also exports injectFontLinks and the shared buildGoogleFontsConfig / buildAdobeFontsConfig builders
[2026-06-10 23:59] minor: Add shared breakpoint-key module (services) — parseBreakpointKey, breakpointKeyMatches, breakpointKeyRangeSize, compareBreakpointSpecificity, breakpointKeyQuery, isBreakpointName, plus BreakpointKey/BreakpointModifier/ParsedBreakpointKey types. Specificity-first precedence (smaller range wins; tie broken exact > down > up) and the new "-"/down max-width query. Shared by withBreakpoints and (next) setTokens $breakpoints
[2026-06-10 23:59] major: setTokens breakpoint values now use the unified +/-/exact shorthand grammar (shared with withBreakpoints). Bare name = exact band (was min-width/"up"); "name+" = up, "name-" = down. Base is optional (omit to override an existing token upward only); all-viewport keys (phone+/desktop-) land in :root. @media blocks are grouped by resolved query (desktop & desktop+ share one) and ordered specificity-first so the most specific wins. Per-breakpoint dimensional primitives (--x__laptop) are no longer emitted on the runtime path
[2026-06-11 00:30] major: Rename setFonts to injectFonts (config type SetFontsConfig to InjectFontsConfig). injectFonts now also accepts a links: LinkAttributes[] param for custom self-hosted / non-Google/Adobe fonts, injected verbatim via injectFontLinks (parity with the React FontLoader links prop)
