[2026-05-19 14:37] major: Rename mechanism-name services to match their dimension. getColorToken/getColorTokenKey/getColorTokenValue → getModeToken*. getSizeToken/getSizeTokenKey/getSizeTokenValue → getBreakpointToken*. setColorTokens → setModeTokens. setSizeTokens → setBreakpointTokens. Internal types renamed: SizeTokens → BreakpointTokens, ColorTokensData → ModeTokensData, SizeTokensData → BreakpointTokensData, SizeEmitResult → BreakpointEmitResult, SizeGroup → BreakpointGroup. Generated files renamed: colorTokensData.ts → modeTokensData.ts, sizeTokensData.ts → breakpointTokensData.ts. Service files renamed: getColorToken.ts → getModeToken.ts, getSizeToken.ts → getBreakpointToken.ts, setColorTokens.ts → setModeTokens.ts, setSizeTokens.ts → setBreakpointTokens.ts. Build-script file renamed: emitSizeTokens.ts → emitBreakpointTokens.ts. Consumers using getColorToken/getSizeToken/setColorTokens/setSizeTokens must update.
[2026-05-19 14:37] major: getConstant returns a bare CSS variable string (was {key, var}). New sibling getConstantKey returns the bare variable name without the var() wrapper. Aligns the constant family with the getToken/getTokenKey/getTokenValue pattern. CssConstantResult type removed. Consumers must drop .var accessors.
[2026-05-18 15:58] patch: Rename internal token source files for clarity — module.color.json → module.modes.json (keyed by mode) and module.size.json → module.breakpoints.json (keyed by breakpoint). Build scripts updated. No consumer-visible change: dist output unchanged.
[2026-05-18 14:46] major: Rename the combined CSS output file from variables.css to tokens.css. The package subpath export changes from ./variables.css to ./tokens.css. Consumers must update @import "nice-styles/variables.css" to @import "nice-styles/tokens.css".
[2026-05-18 12:00] minor: Add generateTokenCSS service — pure JS core of nice-react-styles' createTokens. Builds the :root + @media CSS string from a token map; calls registerTokens + setBreakpoints internally. The React wrapper in nice-react-styles is now a thin shim.
[2026-05-17 12:00] major: Absorb framework-agnostic services from nice-react-styles — registry (registry/registerTokens/seedDimensionedTokens), token resolver (getToken is registry-aware now and throws on unknown), tokenStyleSheet/injectTokenCSS, parseGoogleFontsUrl, isStyleValue, styleValues constants, ModeValue/BreakpointValue/GoogleFonts types. getBreakpointToken reads from the registry. CoreTokenConfig dropped — getToken signature is positional (name, variant?, mode?).
