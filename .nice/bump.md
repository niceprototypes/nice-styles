[2026-05-18 12:00] minor: Add generateTokenCSS service — pure JS core of nice-react-styles' createTokens. Builds the :root + @media CSS string from a token map; calls registerTokens + setBreakpoints internally. The React wrapper in nice-react-styles is now a thin shim.
[2026-05-17 12:00] major: Absorb framework-agnostic services from nice-react-styles — registry (registry/registerTokens/seedDimensionedTokens), token resolver (getToken is registry-aware now and throws on unknown), tokenStyleSheet/injectTokenCSS, parseGoogleFontsUrl, isStyleValue, styleValues constants, ModeValue/BreakpointValue/GoogleFonts types. getSizeToken reads from the registry. CoreTokenConfig dropped — getToken signature is positional (name, variant?, mode?).
