[2026-06-03 20:48] patch: Split module.json token source into per-group files under tokens/modules/
[2026-06-05 13:44] minor: Add breakpoint axis to component tokens — components/*.json now accept $breakpoints, parallel to $themes
[2026-06-05 14:44] minor: Support alt-theme names beyond night — any $themes key emits a [data-theme] pin (modules and components)
[2026-06-08 23:25] minor: Add base-less token shorthands — bare (--np--color, --np--gap) and dimensional (--np--color--night, --np--font-size--desktop) aliases for module + component tokens. Shipped in tokens.css (auto) and standalone "nice-styles/shorthand.css"
[2026-06-09 02:35] major: Reorder button/input status tokens to property-then-state so `base` is terminal, matching the module shape. Renames emitted vars: --np--button--status--primary--background-color → --np--button--status--primary--background-color (same for input). Path lookups must use [status, type, property, state]
[2026-06-09 19:09] minor: Add JS-only font loading — setFonts({ googleFonts, adobeFonts }) injects the same <head> preconnect + stylesheet links as StylesProvider. Also exports injectFontLinks and the shared buildGoogleFontsConfig / buildAdobeFontsConfig builders
[2026-06-10 23:59] minor: Add shared breakpoint-key module (services) — parseBreakpointKey, breakpointKeyMatches, breakpointKeyRangeSize, compareBreakpointSpecificity, breakpointKeyQuery, isBreakpointName, plus BreakpointKey/BreakpointModifier/ParsedBreakpointKey types. Specificity-first precedence (smaller range wins; tie broken exact > down > up) and the new "-"/down max-width query. Shared by withBreakpoints and (next) setTokens $breakpoints
[2026-06-10 23:59] major: setTokens breakpoint values now use the unified +/-/exact shorthand grammar (shared with withBreakpoints). Bare name = exact band (was min-width/"up"); "name+" = up, "name-" = down. Base is optional (omit to override an existing token upward only); all-viewport keys (phone+/desktop-) land in :root. @media blocks are grouped by resolved query (desktop & desktop+ share one) and ordered specificity-first so the most specific wins. Per-breakpoint dimensional primitives (--x__laptop) are no longer emitted on the runtime path
[2026-06-11 00:30] major: Rename setFonts to injectFonts (config type SetFontsConfig to InjectFontsConfig). injectFonts now also accepts a links: LinkAttributes[] param for custom self-hosted / non-Google/Adobe fonts, injected verbatim via injectFontLinks (parity with the React FontLoader links prop)
[2026-06-15 19:11] minor: Add animationEasing variants linear, ease-in, ease-out, ease (base remains ease-in-out)
[2026-06-15 20:05] patch: getTokenValue now resolves breakpoint-driven tokens (e.g. fontSize) to their default (phone) primitive instead of returning the raw BreakpointValue object, which stringified to "[object Object]". Mirrors the existing day-theme flattening.
[2026-06-15 21:00] minor: Add animationDuration variant fast (160ms) and retune base 300→240, slow 600→480. The icon spinner, which derives from animationDuration--slow, speeds up accordingly
[2026-06-15 22:16] minor: Add getHSLA({ module, token, theme, values }) — reads a token's hsla value for the given theme primitive and adds per-channel [h, s, l, a] deltas, clamping each channel to its range (h 0-360, s/l 0-100, a 0-1) with a console.warn on clamp
[2026-06-15 22:38] minor: Add state background colors — background-color success, warning, error, link (very light tints for day, dark for night; emits --np--background-color--{state}--night). Hues match the color module's semantic states
[2026-06-18 18:13] minor: Add colorInverse and backgroundColorInverse token modules — each variant's value is the opposite theme's color (day inverse = the night color, and vice versa), seeded as string literals (not var-wired) so they can be tweaked independently. A region styled with these renders the inverse theme and still flips with prefers-color-scheme / a [data-theme] pin — no JS data-theme toggling. Emits --np--color-inverse--* and --np--background-color-inverse--* (+ --day/--night primitives). Extending to a 3rd theme = add its $themes entry to each inverse file
[2026-06-19 19:24] major: getToken/getTokenKey/getTokenValue, getThemeToken*, getBreakpointToken*, getComponentToken* now take an options object — only the token name (the component prefix for getComponentToken*) stays positional; variant/theme move into {opts}. getToken* gains an inverse option that resolves the matching ...Inverse module (color/backgroundColor). getComponentToken* takes { token, variant, mode } (token may be a path array). getBreakpointToken* takes { variant, breakpoint }. getConstant unchanged.
[2026-06-19 20:10] minor: Add brandColor token module — base hsla(240, 100%, 74.5%, 1) (#7D7DFF) and secondary hsla(198, 100%, 54.1%, 1) (#15B8FF); placeholder brand palette, static across modes (no $themes). Emits --np--brand-color/--secondary (+ shorthand --np--brand-color). Also add borderWidth variants smaller (0.75px) and larger (4px)
[2026-06-19 23:30] major: Inverse colors restructured. The inverse of color/backgroundColor now lives under a reserved $inverse key in the base module (color.json/backgroundColor.json) — the colorInverse/backgroundColorInverse modules are removed. CSS vars change from --np--color-inverse--{variant} to --np--color--{variant}--inverse (a trailing, double-dashed --inverse segment after the theme: --inverse semantic, --day--inverse / --night--inverse primitives). getToken({ inverse: true }) emits the new form; getConstant gains an inverse option. Also: base-less CSS variable shorthands are DEPRECATED — tokens.css no longer appends them, dist/shorthand.css and the ./shorthand.css export are removed (pending a static-name/dynamic-value replacement). Consumers referencing --np--{group} bare aliases or the old --np--*-inverse--* vars must update
[2026-06-20 01:05] patch: getTokenValue now resolves the requested theme — getTokenValue(name, { theme: "night" }) returns the night value, not day. getDefaultVariants folds ThemeValue entries to the requested theme (fallback day) instead of always day. getToken / getTokenKey var references are unchanged (already theme-suffixed)
[2026-06-20 01:30] minor: token getters accept { pristine: true } — resolves against a seed snapshot (defaultsRegistry) captured before any setTokens/registerTokens override. var/key are identical; only the value differs. For canonical-default displays (token reference docs) that must ignore runtime overrides.
[2026-06-21 14:32] major: getToken now takes variant as its second positional argument — getToken(name, variant?, { theme, inverse, pristine }?). Reverts the options-only variant for getToken specifically (its siblings getTokenKey/getTokenValue keep variant inside the options object). Callsites passing { variant } to getToken must move the value to the 2nd positional arg; theme/inverse/pristine stay in the trailing options object
[2026-06-21 16:57] minor: Add input borderWidth component token (--np--input--border-width--*) mapping each variant to the core --np--border-width-- value, so an Input border width can be overridden per-subtree without changing the root border-width token
[2026-06-21 21:06] minor: Add component-scoped border/size/color tokens that mirror the root values — button (borderWidth, borderColor), image (borderWidth, borderColor, backgroundSize), tile (backgroundSize + full backgroundColor variant set), form (gap). Each maps to its core var so a component subtree can override the value without changing the root token
[2026-06-22 15:52] major: Rename the icon spinning-animation-duration component token to animationDuration. CSS var --np--icon--spinning-animation-duration becomes --np--icon--animation-duration; it is now the base speed for all icon animations. getIconToken("spinningAnimationDuration") callers must use getIconToken("animationDuration")
[2026-06-22 16:37] minor: Re-enable base-less CSS variable shorthands. tokens.css again appends the alias block (terminal --np--color -> var(--np--color); midfix --np--color--night -> var(--np--color--night)) for modules and components, dist/shorthand.css is emitted again, and the ./shorthand.css export is restored. Reverses the 2026-06-19 deprecation
[2026-06-22 17:20] major: Make base-less CSS variable names canonical and drop the --base segment entirely. getConstant/getConstantKey, getTokenByPath, and the component-token CSS emitter now omit the base variant (--np--color, --np--color--night, --np--button--size); generated tokens.css and per-group CSS emit only base-less names. Supersedes the 16:37 shorthand re-enable — base-less is now the sole canonical form, not an alias — so emitShorthand, the appended alias block, dist/shorthand.css, and the ./shorthand.css export are all removed. getToken("X","base") / getComponentToken variant "base" keep working (resolve to the base-less name). Consumers referencing var(--np--X--base) must drop the --base segment
