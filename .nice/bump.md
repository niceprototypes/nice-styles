[2026-05-24 06:30] major: Rename `foregroundColor` token group to `color` (align with CSS standards). Top-level key in `module.modes.json` renamed; all `--np--foreground-color--*` CSS variables become `--np--color--*`. Type `ForegroundColorType` renamed to `ColorType`. Component token leaves in `component.json` keyed `foregroundColor` (under tile, button-status, input-status) become `color`; their values that referenced `var(--np--foreground-color--*)` now reference `var(--np--color--*)`. Generated files (`src/generated/types.ts`, `modeTokensData.ts`, `componentTokensData.ts`) regenerate from the renamed JSON sources.
