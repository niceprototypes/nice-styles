# Session 2026-04-21 #0001

**User:** Rename the `borderColor` token variants `heavy` / `heavier` to `dark` / `darker`.

**Claude:** Edited `src/tokens/module.color.json` (both `day` and `night` maps) to rename the variants and ran `npm run build` to regenerate derived artifacts. After the build, `src/generated/colorTokensData.ts`, `src/generated/types.ts` (`BorderColorType = "base" | "dark" | "darker"`), `dist/variables.css`, `dist/color-scheme.css`, and `dist/css/borderColor.css` all emit the new variant names. No hand edits to any generated file.
